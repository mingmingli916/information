\documentclass{article}
\usepackage[a6paper]{geometry}
\usepackage{amsmath}            % provide multline command
\begin{document}
\begin{multline}
  \sum = a + b + c + d + e \\
  + f + g + h + i + j \\
  + k + l + m + n
\end{multline}
% The first line is left-aligned, the last line right-aligned, and all
% other lines in between are centered
\end{document}